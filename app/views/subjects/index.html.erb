<h1 class="text-center">Sujets</h1>
<div class="container">

  <%= link_to 'Ajouter un sujet', new_subject_path, class: 'btn btn-primary' %>

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Parent it</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @subjects.each do |subject| %>
        <tr>
          <td><%= subject.name %></td>
          <td><%= subject.parent_id %></td>
          <td><%= link_to 'Modifier', edit_subject_path(subject) %></td>
          <td><%= link_to 'Détruire', subject, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

</div>
