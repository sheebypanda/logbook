<% compteur = 1 %>

<%= week_calendar do |date| %>
  <!-- Matin -->
  <%= render "half_day", date: date, compteur: compteur, morning: true %>
  <hr>
  <!-- Soir -->
  <%= render "half_day", date: date, compteur: compteur, morning: false %>
  <% compteur += 1 %>
<% end %>
